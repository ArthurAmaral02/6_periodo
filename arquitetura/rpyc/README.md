No meu linux é esse passo a passo

```
python -m venv .venv

source .venv/bin/activate.fish

pip install rpyc

cd as_unicap-main/praticas/rpc_pratica/

python servidor.py # <- isso em um terminal

# depois

python cliente.py # <- em outro terminal

# Alem disso incrementei as funções de ordenar um vetor int e de fazer um anagrama de uma palvra
```
